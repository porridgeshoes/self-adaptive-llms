# Transformer²的工作有哪些？
- 提出奇异值微调。对比LORA，参数更少，效果更好。并且他是基于RL的端到端调整奇异值。
- 提出3种自适应方案，即如何选择合适的微调后的奇异值插件。
   - 基于prompt提示来选择其中一个；
   - 额外微调了一个用于分类任务的奇异值权重；
   - 提出权重组合算法。交叉熵方法（CEM）是一种蒙特卡洛方法，比较简单的思想。

# 训练日志i记录
## 00训练-非acce启动、无nvcc禁用：vllm默认加载到卡0，其他都只加载到卡1，在训练step阶段报错r.nvmlDeviceGetNvLinkRemoteDeviceType_；
   - vLLM初始版本加载后：
      - 卡0: 27479MiB / 40537MiB
   - AutoModelForCausalLM导入模型结构和参数后：
      - 卡0: 27479MiB / 40537MiB
      - 卡1: 15129MiB / 40537MiB
   - 结束v.to(torch.bfloat16).to(gpu)后：
      - 卡0: 27479MiB / 40537MiB
      - 卡1: 33505MiB / 40537MiB
   - 结束DOptimizationAlgorithm初始化后：
      - 卡0: 27479MiB / 40537MiB
      - 卡1: 33583MiB / 40537MiB
   - step（）
## 01训练-非acce启动，禁用nvcc：vllm启动时占用内存大了些，且139行AutoModelForCausalLM导入模型这一步就失败了！！！
   - vLLM初始版本加载后：
      - 卡0: 32887MiB / 40537MiB
   - AutoModelForCausalLM导入模型结构失败：
   RuntimeError: CUDA error: invalid device ordinal
   Compile with `TORCH_USE_CUDA_DSA` to enable device-side assertions.
   - 代码指定了一个超出实际可用GPU数量范围的设备编号。例如，如果你的系统只有4个GPU（编号0-3），但代码尝试访问cuda:4。

## 13训练-acce启动，无nvcc禁用， 采用后续修改的卡序号管理3+1模式：vllm加载超时
  - UserWarning: Failed to get the IP address, using 0.0.0.0 by default.The value can be set by the environment variable VLLM_HOST_IP or HOST_IP.
  get_ip(), get_open_port())
  - 加载vLLM超时。卡0 1245MiB / 40537MiB

## 12训练-acce启动，禁用nvcc， 采用后续修改的卡序号管理3+1模式：vllm加载超时
   - 加载vLLM超时。卡0 1245MiB / 40537MiB


# 一些草稿命令
## 一键创建项目环境
cd /data1/yammyjiang/new_space/light_things/LLM/self-adaptive-llms
bash -x scripts/create_env.sh
## nvcc环境问题
```bash
Error executing job with overrides: ['base_model@_global_=qwq32b', 'task@_global_=gsm8k', 'mode@_global_=training', 'num_iters=1']
Traceback (most recent call last):
  File "ccs/svd_reinforce_hydra.py", line 318, in main
    optimization_algorithm.step_optimization(
  File "/data1/yammyjiang/new_space/light_things/LLM/self-adaptive-llms/optim_modules.py", line 139, in step_optimization
    new_params = forward(
                 ^^^^^^^^
  File "/root/miniconda3/envs/t2/lib/python3.11/site-packages/torch/utils/_contextlib.py", line 116, in decorate_context
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
  File "/data1/yammyjiang/new_space/light_things/LLM/self-adaptive-llms/utils.py", line 131, in forward
    new_params[k] = compose_new_params(
                    ^^^^^^^^^^^^^^^^^^^
  File "/data1/yammyjiang/new_space/light_things/LLM/self-adaptive-llms/utils.py", line 115, in compose_new_params
    decomposed_params[f"{param_name}.U"]
RuntimeError: r.nvmlDeviceGetNvLinkRemoteDeviceType_ INTERNAL ASSERT FAILED at "../c10/cuda/driver_api.cpp":33, please report a bug to PyTorch. Can't find nvmlDeviceGetNvLinkRemoteDeviceType: /usr/lib/x86_64-linux-gnu/libnvidia-ml.so.1: undefined symbol: nvmlDeviceGetNvLinkRemoteDeviceType
```
- **现象**：系统中找不到NVCC：command not found: nvcc
- **原因**：
   - NVCC缺失：NVCC (NVIDIA CUDA Compiler) 是CUDA工具包的一部分，用于编译CUDA代码。找不到它意味着您没有完整的CUDA工具包安装或环境变量设置不正确
   - openr1也出现这样的问题。本质是因为驱动版本太老，装不上nvcc，而allblue这些容器又不让装；当gpu利用率太高时，就会触发。
- **解决方案**：
   - nanzhi让控制gpu利用率；nanzhi让申请8卡的容器；nanzhi说这个必须集群来解决，因为需要在”x数x“注入阶段就要把驱动装好。；nanzhi也尝试关闭nvcc的功能，但是一直没用；
   - # 运行脚本前设置这些环境变量
   export NCCL_P2P_DISABLE=1
   export NCCL_IB_DISABLE=1
- 来自cursor第二次尝试：
   ```bash
   根据查看的代码和错误信息，这个问题与NVIDIA NVLink功能和NVML (NVIDIA Management Library) 相关。在修改脚本之外，我们可以考虑修改代码中与CUDA/PyTorch设备相关的部分，以避免直接使用可能触发问题的NVLink功能。
   我们已经在脚本中添加了PYTORCH_NO_NVML=1环境变量，这应该可以阻止PyTorch初始化NVML。此外，我们还可以修改下面的部分，使用更安全的方式初始化PyTorch分布式训练：让我们来修改svd_reinforce_hydra.py中的初始化部分。
   这个解决方案通过以下方式避免NVLink相关错误：
   1. 完全禁用了NVLink通信功能
   2. 对可能触发NVLink/NVML错误的操作（如SVD分解和张量重组）进行了保护
   3. 在遇到问题时会自动切换到CPU执行这些操作
   4. 保留了多GPU训练能力，但使用更安全的通信方式
   ```
- cursor对于第二点的解释：这些设置禁用了可能出问题的高速直连通信方式（如NVLink），但保留了通过系统内存或网络进行的多GPU通信能力。这种方法实际上是一种"降级"的多GPU训练方式 - 与完全启用了高速GPU互联（如NVLink）的训练相比，它的通信效率会低一些，但相比完全放弃多GPU训练要好得多。
```bash
   # 禁用GPU之间的点对点直接通信，但仍允许通过主机内存进行通信
   export NCCL_P2P_DISABLE=1
   
   # 禁用InfiniBand通信，转而使用以太网
   export NCCL_IB_DISABLE=1
   
   # 完全禁用NVLink通信，但不影响其他通信方式
   export NCCL_NVLINK_DISABLE=1
   
   # 指定网络接口和Socket通信线程数
   export NCCL_SOCKET_IFNAME=eth0
   export NCCL_SOCKET_NTHREADS=8
   
   # 禁用共享内存传输，使用更安全但可能稍慢的通信方式
   export NCCL_SHM_DISABLE=1
```


## 创建新环境
```bash
# 基于nanzhi1340镜像
conda create --name t2 --clone torch
conda activate t2
cd /data1/yammyjiang/new_space/light_things/LLM/self-adaptive-llms
pip install --upgrade pip

# 安装本地下载的 evalplus
pip install /data1/yammyjiang/new_space/heavy_things/self-adaptive-llms/download/evalplus-0.3.0.dev16.zip

# 删除requirements.txt里的evalplus，再安装。
pip install -r requirements.txt

# 安装项目自创的fishfarm
cd evaluation/fishfarm
pip install -e .

# evalplus的安装工比较特别；需要先下载再本地安装
pip download evalplus@git+https://github.com/evalplus/evalplus@1895d2f6aa8895044a7cf69defc24bd57695e885
```
## 运行出错
- load_datasets 无法下载数据，调整为全局缓存不生效，依然采用函数中执行缓存路径的方法；
- qwen模型加载出错。 transformer等版本原因。调整一下包版本与nanzhi13的一致。毕竟他的环境可以正常运行qwen模型。
```bash
flash_attn                        2.7.4.post1
torch                             2.5.1
torchaudio                        2.5.1
torchvision                       0.20.1
transformers                      4.48.3
```
- vllm和torch环境褒姒冲突了。升级为nanzhi 1340的LLM版本
```bash
pip install vllm==0.7.2
```
- 奇异值分解报错，维度不匹配，猜测可能原因是。由于用的大模型不一样，每层参数的名称跟作者的不一样。作者通过名称来过滤，我改为根据维度来过滤。
```
U, S, V = torch.svd(v)
              ^^^^^^^^^^^^
RuntimeError: linalg.svd: input should have at least 2 dimensions, but has 1 dimensions instead 
# 改为：
if v.dim() < 2:
   print(f"Skipping {k} (shape: {v.shape}): requires at least 2D for SVD.")
   continue  # 跳过当前参数，继续下一个循环
```

# 关于 `self.dec_param_file_n = "llama3_decomposed_params.pt"` 的解析
看不懂.pt是个路径还是文件名？（解：是个文件名，存在奇异值分解的结果）

这行代码定义了一个文件名，用于存储 Llama3 模型的分解参数（decomposed parameters）。让我详细解释其含义和用途：

## 代码含义

`self.dec_param_file_n = "llama3_decomposed_params.pt"` 这行代码设置了一个变量，它指定了一个文件名，该文件用于保存和加载模型的分解参数。

具体解释：
- `dec_param_file_n`：变量名，表示"decomposed parameters file name"（分解参数文件名）的缩写
- `"llama3_decomposed_params.pt"`：文件名，指向存储Llama3模型分解参数的文件

## 为什么是 .pt 结尾？

`.pt` 文件扩展名是 PyTorch 框架中常用的文件扩展名，用于保存 PyTorch 模型或张量。具体来说：

1. **PyTorch 标准格式**：`.pt` 和 `.pth` 是 PyTorch 保存模型和参数的标准扩展名
2. **张量序列化**：这些文件包含了使用 PyTorch 的 `torch.save()` 函数序列化的张量数据
3. **适合保存复杂数据**：`.pt` 文件可以保存复杂的嵌套字典、列表和张量结构

## 在 Transformer² 框架中的作用

在这个自适应 LLM 框架中，这个文件特别重要，因为：

1. **SVD 分解数据**：它存储了对模型权重矩阵进行奇异值分解(SVD)后得到的结果
   - 通常包含 U（左奇异向量）、S（奇异值）和 V（右奇异向量）
   - 这是框架的核心技术 —— 通过只修改奇异值而保持奇异向量不变，可以高效地调整模型行为

2. **避免重复计算**：SVD 分解是计算密集型操作，特别是对于大型模型
   - 项目会检查这个文件是否存在（查看 svd_reinforce_hydra.py 中的 `extract_svd = cfg.extract_svd or (not os.path.exists(decomposed_param_file))`）
   - 如果文件存在，直接加载已计算的分解结果，避免重新计算

3. **参数共享**：所有针对特定类型错误的"专家"模型都基于相同的分解参数，只是调整奇异值不同

## 实际应用中的工作流程

1. 首次使用新模型时，系统会执行 SVD 分解并将结果保存到这个 `.pt` 文件
2. 后续运行时，系统直接加载这个文件以获取分解参数
3. 训练"专家"模型时，只调整奇异值，保存在单独的专家参数文件中
4. 推理时，系统加载基础分解参数和专家参数，动态组合它们以适应输入

从项目的 `.gitignore` 文件中也可以看到 `*_decomposed_params.pt` 被忽略，这表明这些文件通常会在本地生成，而不是版本控制的一部分，因为它们可能很大且可以从原始模型重新生成。


# Hydra配置系统解析
- 看不懂为什么可以通过=的形式给python脚本传参? 
   - Hydra包就是这种传参形式。--是另一包的传参形式；
   - Hydra包还可以将这些参数配置进行混合。覆盖原有的配置；
   - 这个项目的cfg文件夹遵循的就是Hydra包的参数管理形式。
- @ 符号又是什么神奇的解释？
   - Hydra的特色；从配置组选择其一进行生效。

CUDA_VISIBLE_DEVICES=0,1 python svd_reinforce_hydra.py \
    base_model@_global_=llama3i8b \
    task@_global_=$TASK \
    mode@_global_=eval \
    prompt_based_eval=True \
    experts_path_dict.code=$CODE_EXPERT_PATH \
    experts_path_dict.math=$MATH_EXPERT_PATH \
    experts_path_dict.reasoning=$REASONING_EXPERT_PATH \
    load_ckpt=$CLS_EXPERT_PATH

这段代码是在使用Hydra配置系统传递参数给Python脚本`svd_reinforce_hydra.py`。让我详细解释这些配置参数及其工作方式：

## @符号的特殊含义

在Hydra配置系统中，`@`符号用于指定配置组（config group）的覆盖。当看到`base_model@_global_=llama3i8b`时，这表示：

1. `base_model`是配置组的名称
2. `@_global_`表示这是一个全局覆盖（覆盖默认配置）
3. `=llama3i8b`指定要使用的具体配置文件（即`cfgs/base_model/llama3i8b.yaml`）

## 参数传递机制

这些参数是通过命令行传递给`svd_reinforce_hydra.py`脚本的。Hydra会解析这些参数并：

1. 加载默认配置（在`cfgs/config.yaml`中定义）
2. 根据命令行参数覆盖指定的配置项
3. 将合并后的配置对象传递给脚本的主函数

## 具体参数解析

```bash
base_model@_global_=llama3i8b
```
- 覆盖默认的基础模型配置，使用`cfgs/base_model/llama3i8b.yaml`中的配置

```bash
task@_global_=$TASK
```
- 覆盖默认的任务配置，使用`cfgs/task/$TASK.yaml`中的配置
- `$TASK`是一个环境变量，会在脚本执行前设置

```bash
mode@_global_=eval
```
- 设置运行模式为评估模式，使用`cfgs/mode/eval.yaml`中的配置

```bash
prompt_based_eval=True
```
- 直接设置配置变量`prompt_based_eval`为`True`，这会覆盖默认配置中的该值

```bash
experts_path_dict.code=$CODE_EXPERT_PATH
experts_path_dict.math=$MATH_EXPERT_PATH
experts_path_dict.reasoning=$REASONING_EXPERT_PATH
```
- 使用点符号设置嵌套配置`experts_path_dict`中的各个字段
- 这些路径指向不同任务类型（代码、数学、推理）的专家模型参数文件

```bash
load_ckpt=$CLS_EXPERT_PATH
```
- 设置`load_ckpt`变量，指向分类专家模型的参数文件路径

## 整体流程

1. 脚本执行时，Hydra会解析这些命令行参数
2. Hydra加载默认配置并应用这些覆盖
3. `svd_reinforce_hydra.py`接收最终合并的配置对象
4. 脚本根据配置加载指定的模型和专家参数
5. 执行基于提示（prompt-based）的评估过程

这种配置方式的好处是灵活性高、可读性强，允许用户通过简单的命令行参数切换不同的模型、任务和评估模式，而不需要修改代码。

# vllm加载大模型
```python
# 在线形式
model = vllm.LLM(
    model_id,                     # 模型的唯一标识符（例如 "gpt-3.5-turbo" 或本地模型路径）
    max_model_len=1024,           # 模型的最大上下文长度（token 数量）
    gpu_memory_utilization=0.8,   # GPU 内存的利用率（0.8 表示 80%）
    enforce_eager=True,           # 是否强制使用 eager 模式（可能用于调试或性能优化）
    dtype="bfloat16",             # 模型的计算精度（这里是 bfloat16）
    download_dir=get_download_dir(), # 模型下载的目录（如果模型需要从远程下载）
)
# 离线形式
model = vllm.LLM(
    model_id="/path/to/local/model",  # 指定本地模型路径
    max_model_len=1024,              # 最大上下文长度
    gpu_memory_utilization=0.8,      # GPU 内存利用率
    enforce_eager=True,              # 强制使用 eager 模式
    dtype="bfloat16",                # 计算精度
    download_dir=None                # 不需要下载目录，设置为 None
)
```


# 神奇的初始化一个类实例的方式：通过配置文件进行初始化。
``` python
policy: Policy = hydra.utils.instantiate(
    cfg.shakeoff_policy,
    base_params=base_params,
    decomposed_params=decomposed_params,
    gpu=gpu,
)
class Policy(nn.Module):
    def __init__(self, base_params, gpu, init_val, max_mult=1, **kwargs):
```
```bash
shakeoff_policy:
  _target_: policy.Policy
  init_val: ${init_val}
  mode: ${policy_mode}
  max_mult: ${max_mult}
```

# 理解 `@abc.abstractmethod` 和 `step_optimization` 函数

这段代码是一个抽象方法的定义，属于 `self-adaptive-llms` 项目中的优化模块部分。让我来解释它的含义和作用：

## `@abc.abstractmethod` 装饰器的作用

`@abc.abstractmethod` 是 Python 中的抽象方法装饰器，它有以下作用：

1. **定义抽象方法**：标记一个方法为抽象方法，这意味着这个方法只有声明而没有具体实现。
2. **强制子类实现**：任何继承包含这个抽象方法的类的子类，必须实现这个方法，否则无法实例化该子类。
3. **接口设计**：它帮助定义一个"接口"或"协议"，确保所有实现类都遵循相同的方法签名。

## `step_optimization` 函数的作用

从代码和项目上下文来看，`step_optimization` 函数是优化算法的核心步骤，它的作用是：

1. **执行优化迭代**：在每个训练步骤中执行模型参数的优化。
2. **处理模型适应**：根据特定任务调整大型语言模型的参数。

函数参数说明：
- `model_id`：模型的标识符
- `model`：要优化的神经网络模型
- `tokenizer`：文本标记化器
- `policy`：控制参数更新的策略
- `task_loader`：加载任务数据的组件
- `batch_ix`：当前批次的索引
- `train_data`：训练数据
- `train_eval`：训练评估函数
- `base_params`、`decomposed_params`、`original_model_params`：不同形式的模型参数
- `metrics_to_log`：需要记录的指标
- `vllm_model`：可选的 VLLM 模型参数

## 在 Transformer² 项目中的作用

根据项目文件，这个函数是 `Transformer²`（自适应 LLM）框架中的核心组件之一。该框架通过动态调整模型的奇异值分解组件，使大型语言模型能够实时适应不同任务。

`step_optimization` 函数在不同的优化算法实现类中（如 `Reinforce`、`CEM`、`RandomShooting` 等）会有不同的具体实现，负责执行各自优化算法的一步迭代。这些类都在 `optim_modules.py` 文件中定义，并且都继承了包含这个抽象方法的基类。

从项目结构看，这是一个典型的策略模式实现，允许在运行时选择不同的优化算法，同时保持统一的接口。

要确定执行这个训练脚本时使用的优化算法配置，需要查看 Hydra 配置系统中的默认设置。

# 怎么看默认的优化算法配置的是什么？
要确定使用的默认优化算法，需要查看项目的 Hydra 配置文件。根据之前看到的配置文件结构，默认配置应该在 `cfgs/config.yaml` 中定义。
从 `cfgs/config.yaml` 中可以看到默认值:
```yaml
defaults:
  - _self_
  - policy@_global_: default
  - task@_global_: gsm8k
  - base_model@_global_: llama3i8b
  - optimization@_global_: reinforce
  - mode@_global_: training
```
可以看出，默认的优化算法配置是 `reinforce`，对应于 `cfgs/optimization/reinforce.yaml` 文件中的配置。

因此，当您执行 `train_task_expert_yam.sh` 脚本时，使用的优化算法是 **Reinforce 强化学习算法**，其具体配置在 `cfgs/optimization/reinforce.yaml` 文件中定义:

如果您想使用不同的优化算法，可以在执行脚本时添加参数，例如:
```shell
python svd_reinforce_hydra.py optimization@_global_=cem ...
```
这将使用 `cfgs/optimization/cem.yaml` 中定义的交叉熵方法(CEM)优化算法。

# 理解 `svd_reinforce_hydra.py` 中的代码片段

这段代码是 `self-adaptive-llms` 项目中训练循环的核心部分，我来详细解释一下：

## 代码逐行解析

```python
for i in range(num_iters):
```
这是一个标准的 Python 循环，重复执行 `num_iters` 次训练迭代。`num_iters` 在您的训练脚本中被设置为 1。

```python
batch_ix = np_random.choice(train_ix, size=clipped_batch_size, replace=False)
```
这行代码从训练数据索引 `train_ix` 中随机抽取 `clipped_batch_size` 个样本，形成一个训练批次：
- `np_random` 是一个 NumPy 随机数生成器实例
- `train_ix` 包含所有可用训练数据的索引
- `clipped_batch_size` 是实际使用的批次大小
- `replace=False` 确保不会重复抽取同一个样本

```python
optimization_algorithm.step_optimization(
    model_id=model_id,
    model=model,
    tokenizer=tokenizer,
    policy=policy,
    task_loader=task_loader,
    batch_ix=batch_ix,
    train_data=train_data,
    train_eval=train_eval,
    base_params=base_params,
    decomposed_params=decomposed_params,
    original_model_params=original_model_params,
    metrics_to_log=metrics_to_log,
    vllm_model=vllm_model,
)
```
这是最核心的部分，调用了优化算法的 `step_optimization` 方法执行一步参数优化。根据您之前的脚本，这里使用的是 Reinforce 强化学习算法。参数含义如下：

- **model_id**: 模型的标识符，例如 "qwq32b"
- **model**: 正在训练的语言模型实例
- **tokenizer**: 用于文本处理的分词器
- **policy**: 参数调整策略，控制如何修改模型权重
- **task_loader**: 任务数据加载器，提供训练和评估数据
- **batch_ix**: 刚才随机选择的训练样本索引
- **train_data**: 完整的训练数据集
- **train_eval**: 用于评估训练效果的函数
- **base_params**: 模型的基础参数
- **decomposed_params**: 经过奇异值分解(SVD)的模型参数
- **original_model_params**: 原始的未修改模型参数
- **metrics_to_log**: 需要记录的性能指标
- **vllm_model**: 可选的VLLM模型实例，用于加速推理

## 工作原理

在 Transformer² 框架中，这段代码的工作流程如下：

1. **随机抽样**: 从训练数据中随机抽取一批样本`总担心训练集没有用完，但是，深度学习确实会随机从训练集抽几个bacthsize，但是抽很多轮的情况`
2. **优化步骤**: 将这批样本传递给优化算法进行一步参数更新
3. **参数调整**: 在优化步骤中，系统会：
   - 使用当前模型参数处理批次数据
   - 计算性能指标和奖励信号
   - 根据强化学习算法更新模型参数
   - 动态调整奇异值分解后的参数向量

这个训练循环体现了论文中描述的自适应LLM的工作机制：通过选择性地调整模型奇异值分解的组件，使模型能够动态适应不同任务，而无需完全重新训练整个模型。

在您使用的REINFORCE算法中，系统会根据模型在任务上的表现计算梯度，并应用这些梯度来优化"专家"参数向量，这些向量控制如何修改原始模型的权重。

# 奇异值分解(SVD)
## 基础知识
在奇异值分解(SVD)中，对于一个 m×n 的矩阵 A，可以分解为三个矩阵的乘积：A = UΣV^T

各个矩阵的维度如下：

1. **原始矩阵 A**：
   - 维度为 m×n

2. **左奇异矩阵 U**：
   - 维度为 m×m
   - 是一个正交矩阵（U^T U = I）
   - 列向量是 AA^T 的特征向量

3. **奇异值矩阵 Σ**：
   - 维度为 m×n
   - 是一个对角矩阵，只有主对角线上有值
   - 对角线上的元素是奇异值，按从大到小排列
   - 奇异值个数为 min(m,n)
   - 其余位置都是0

4. **右奇异矩阵 V^T**：
   - V^T 的维度为 n×n
   - V 是一个正交矩阵（V^T V = I）
   - V 的列向量是 A^T A 的特征向量

用数学符号表示：
\[
A_{m×n} = U_{m×m} \Sigma_{m×n} V^T_{n×n}
\]

注意：有时也会使用简化版本的SVD（截断SVD），这种情况下矩阵的维度会更小，只保留前 r 个奇异值（r 是矩阵的秩）。

## 在 PyTorch 中的 `torch.svd()` 实现与标准的数学表达式有一些细微的差别：

1. **数学表达式中**：
对于矩阵 A(m×n)，我们有：
\[
A_{m×n} = U_{m×m} \Sigma_{m×n} V^T_{n×n}
\]

2. **PyTorch 实现中**：
对于输入矩阵 A(m×n)，`torch.svd()` 返回：
- U 的维度是 (m×min(m,n))
- S 的维度是 (min(m,n))
- V 的维度是 (n×min(m,n))

在你的具体例子中：
- 原始矩阵维度是 [512, 3584]，所以 m=512, n=3584
- min(m,n) = min(512, 3584) = 512
- 因此：
  - U: [512, 512]
  - S: [512]
  - V: [3584, 512]

这是因为 PyTorch 默认返回"简化"或"经济"形式的 SVD（也称为 thin SVD），这种形式去掉了那些对应于零奇异值的冗余列，使计算更加高效。这种形式在实际应用中更常用，因为它包含了所有必要的信息，同时节省了存储空间和计算资源。

如果要得到完整形式的 SVD，需要使用 `torch.svd(v, some=False)`，但在大多数应用中并不需要这样做。

# 为什么scaled_loss拥有backward()方法？

`scaled_loss.backward()`这行代码之所以能够执行，是因为`scaled_loss`是一个PyTorch的张量(Tensor)，且这个张量具有`计算图(computational graph)`。在PyTorch中，所有满足以下条件的张量都可以调用`backward()`方法：

1. 张量是标量（一个单一的值）或者可以被规约为标量
2. 这个张量是通过一系列需要计算梯度的操作创建的
3. 这些操作至少涉及一个设置了`requires_grad=True`的张量

## `scaled_loss`的来源

在Reinforce类的`step_optimization`方法中，`scaled_loss`是这样计算出来的：

```python
# 计算策略梯度损失
pg = -log_likelihood * rewards[j]
loss = pg

# 如果启用KL损失，加入KL散度约束
if use_kl_loss:
    ref_log_probs = ref_log_probs_list[j].to(gpu)
    kl_div = F.kl_div(
        input=log_probs,
        target=ref_log_probs,
        log_target=True,
        reduction="sum",
    )
    loss = loss + kl_ref_coeff * kl_div

# 对损失进行缩放
scaled_loss = loss / clipped_batch_size
```

这个`scaled_loss`作为最终的损失，保留了从模型参数到损失值的完整计算图。

## 计算图是如何构建的

在这个过程中，计算图是这样构建的：

1. 首先获取模型参数：`learnable_params = policy.get_learnable_params()`
2. 通过forward函数生成新参数并载入模型：`new_params = forward(...)`
3. 使用这个模型生成文本并计算`log_probs`
4. 计算对数似然：`log_likelihood = selected_log_probs.sum(axis=-1)`
5. 计算策略梯度损失：`pg = -log_likelihood * rewards[j]`
6. 可选地添加KL散度损失
7. 最后得到`scaled_loss`

这整个过程形成了一个从策略参数到最终损失值的计算图。因为最初的策略参数设置了`requires_grad=True`（在Reinforce初始化时：`self.optimizer = torch.optim.Adam(policy.trainable_params, lr=lr)`），所以PyTorch会跟踪这条计算路径上的所有操作。

## backward()方法做了什么

当调用`scaled_loss.backward()`时，PyTorch会：

1. 从`scaled_loss`出发，向后计算计算图中每个节点的梯度
2. 将这些梯度累积到相应的张量的`.grad`属性中
3. 特别是，会计算策略参数（`policy.trainable_params`）相对于损失的梯度

在Reinforce算法中，这个梯度正是我们需要的更新方向，告诉我们如何调整策略参数以增加获得奖励的概率。

## 后续操作

值得注意的是，在`step_optimization`方法结束后，会调用`backward`函数：

```python
backward(policy, model, base_params, decomposed_params, learnable_params)
```

这个函数在`utils.py`中定义，它会将梯度从模型的MLP参数传播回策略网络的参数。然后在`update`方法中进行参数更新：

```python
def update(self, policy):
    max_grad_norm = self.max_grad_norm
    torch.nn.utils.clip_grad_norm_(policy.trainable_params, max_grad_norm)
    self.optimizer.step()
    self.optimizer.zero_grad()
```

这个设计使得整个训练过程可以通过反向传播有效地更新策略网络参数。

总结：`scaled_loss.backward()`之所以可行，是`因为PyTorch自动微分系统跟踪了从策略参数到损失值的完整计算路径`，使得我们可以计算梯度并实现强化学习中的策略梯度算法。

# 理解 compose_new_params 函数的 backward 调用

这段代码是实现反向传播的关键部分，具体发生了一种称为"自定义梯度流"的过程。让我来详细解释：

```python
compose_new_params(policy, k, decomposed_params, learnable_params).backward(
    model.get_parameter(k).grad, retain_graph=True
)
```

## 代码流程解析

1. **参数重组**：`compose_new_params(policy, k, decomposed_params, learnable_params)` 函数基于SVD分解的组件(U, S, V)和可学习的掩码(mask)重新组合模型参数
  
2. **创建新参数**：这个函数返回一个计算图中的张量，该张量表示模型中某个参数(名为k)的新值

3. **反向传播**：对这个新参数调用 `.backward(model.get_parameter(k).grad)` 方法

4. **保留计算图**：参数 `retain_graph=True` 确保计算图不会被释放，因为后续还需要计算其他参数的梯度

## 特殊之处：自定义梯度流

这段代码特别之处在于 `.backward()` 函数的参数：通常调用 `.backward()` 时不需要提供参数，它默认设置梯度为1.0。但这里提供了 `model.get_parameter(k).grad`，这意味着：

**我们不是从损失出发计算梯度，而是人为指定一个梯度方向，并将其传递回策略网络的参数**。

## 梯度传递流程

完整流程是：

1. 在Reinforce的`step_optimization`方法中，我们已经计算了模型参数相对于损失的梯度，存储在`model.get_parameter(k).grad`中

2. 现在需要将这些梯度传回策略网络的参数，但两者之间存在SVD分解的转换关系

3. 通过调用`compose_new_params(...).backward(model.get_parameter(k).grad)`，我们：
   - 告诉PyTorch：假设`compose_new_params`的输出收到了`model.get_parameter(k).grad`这个梯度
   - PyTorch会自动沿着计算图向后传播，计算`policy`的参数收到的梯度

## 一个类比

可以把这个过程理解为一个链条传递：

1. 先前步骤中：损失 → 模型参数的梯度
2. 这一步骤中：模型参数的梯度 → 策略网络参数的梯度
3. 后续step中：根据策略网络参数的梯度更新策略网络

## 为什么需要这样做？

在这个框架中，我们不是直接优化模型参数，而是优化一个策略网络，该网络生成用于修改模型参数的掩码。这种设计允许：

1. **参数高效**：只需优化少量掩码参数，而不是全部模型参数
2. **保持结构**：通过SVD分解保持了模型权重矩阵的主要结构特性
3. **梯度正确传递**：确保强化学习的梯度信号能正确地从任务奖励传递到策略网络

总之，这行代码实现了一种智能的梯度重定向机制，使得大模型的低秩自适应优化成为可能，是整个Transformer²框架的核心技术之一。

# 为什么最后一个键需要特殊处理？

在这段代码中，最后一个键(key)的特殊处理主要体现在 `retain_graph` 参数上：

```python
# 对所有中间键保留计算图
for k in keys_to_backprop[:-1]:
    compose_new_params(policy, k, decomposed_params, learnable_params).backward(
        model.get_parameter(k).grad, retain_graph=True
    )
    
# 处理最后一个键时释放计算图
compose_new_params(policy, last_key, decomposed_params, learnable_params).backward(
    model.get_parameter(last_key).grad, retain_graph=False
)
```

## 原因：释放计算图内存

最后一个键被特殊处理的根本原因是**内存管理**。使用 `retain_graph=False`（这也是默认值）意味着在完成这次反向传播后，PyTorch会释放整个计算图占用的内存资源。

具体来说：

1. **中间键的处理**：对于所有中间键，使用 `retain_graph=True` 是因为我们还需要继续使用计算图进行其他参数的梯度计算

2. **最后一个键的处理**：当处理到最后一个键时，后续不再需要这个计算图，因此可以安全释放，以节省内存

## 为什么需要这样特殊处理？

这种处理方式解决了几个关键问题：

### 1. 内存效率

计算图可能非常大，尤其是对于大型语言模型。如果不释放计算图，会导致大量内存被占用，可能引发OOM（内存不足）错误。

```
retain_graph=False  # 释放内存，避免内存泄漏
```

### 2. 计算效率

在PyTorch中，每次调用backward时都会累积梯度。通过这种方式处理，可以确保每个参数的梯度正确计算而不会重复累积。

### 3. 技术需求

在PyTorch的autograd系统中，一个计算图正常情况下只能进行一次反向传播。通过在中间步骤设置 `retain_graph=True`，我们可以绕过这个限制，对不同参数分别进行反向传播。

## 代码整体逻辑

完整的backward函数逻辑是：

1. 筛选需要进行反向传播的参数键（只处理MLP层的参数）
2. 找出最后一个需要处理的键
3. 对除最后一个键外的所有键，进行带有 `retain_graph=True` 的反向传播
4. 对最后一个键进行 `retain_graph=False` 的反向传播，释放计算图

这种实现方式在处理复杂模型时非常常见，尤其是当需要对不同参数组分别进行反向传播时。在Transformer²框架中，这种设计允许系统高效地将梯度从模型参数传播到策略网络参数，同时避免不必要的内存占用。

总而言之，对最后一个键的特殊处理是一种优化内存使用的重要技术手段，也是实现复杂梯度流动的必要环节。

# 单机4卡，出现内存爆炸以及还有两个闲置的问题；
采用原项目训练脚本，仅将CUDA_VISIBLE_DEVICES=0,1,改为了CUDA_VISIBLE_DEVICES=0,1,2,3。
- **原因**：本项目限制了只用其中一张卡进行训练：
   在 svd_reinforce_hydra.py 文件中，有几处硬编码的GPU设备指定：
   第122行: gpu = torch.device("cuda:1") - 这将主要计算设备固定为cuda:1
   第133行: 模型加载时指定 device_map="cuda:1" - 这也将模型加载限制在特定的GPU上
- **cursor处理**：单卡限制解除，避免卡闲置。
   修改了 svd_reinforce_hydra.py 文件中的硬编码GPU设备指定：
   将 gpu = torch.device("cuda:1") 改为 gpu = torch.device("cuda")，让PyTorch自动选择设备
   将模型加载时的 device_map="cuda:1" 改为 device_map="auto"，这样Hugging Face的模型加载会自动将模型分布到多个可用的GPU上
- **我的处理**：借鉴openr1的形式，用zero3进行显存加速，避免内存爆炸；

# 采用zero3并更改进程数为4，但是程序卡住了；
在gsm8k.py的get_vllm_model方法中，我们可以看到VLLM初始化时没有设置tensor_parallel_size参数，默认值为1，这导致即使你有多个GPU，也只会使用一个GPU进行推理。让我们修改GSM8K任务中的get_vllm_model方法以使用多个GPU


# CUDA_VISIBLE_DEVICES=0,1,2,3方式4张A100卡（每张卡显存40G）训练7B模型出现内存溢出。（1张卡内存溢出，其他卡gpu闲置）。
在驱动版本低且不支持nvcc通信的情况下，直接使用CUDA_VISIBLE_DEVICES方式确实很难成功训练7B模型，出现的内存溢出问题是有具体原因的。
1. 只用pytorch的情况下，他不会自动去优化模型参数（28g）、参数梯度（28g*n）、优化器状态（28g*n）应该加载在哪张卡，默认都会加载到第一张卡(GPU 0)上。自然就第一张卡显存爆炸；
2. 非得只用pytorch不用accelerate+zero3，需要手动更改代码，手动将模型参数、梯度、优化器状态等分别加载到每张卡上。
   **如果坚持使用直接方式，必须：**
      - 修改模型代码，实现手动模型并行
      - 大幅减小批量大小
      - 使用更激进的混合精度和梯度检查点技术
      - 但这需要对代码进行大量修改，不现实
   `这些操作就都是Accelerate+DeepSpeed专门做的事！！！`

# 使用Accelerate启动时，看起来每条info信息都打印了4遍，看起来每张卡都在独立跑这个程序，这样的话，他们之间如何配合呢？
- Accelerate启动时，为每张GPU卡创建一个独立的Python进程。
- 默认情况下，每个进程会独立执行代码中的print语句，从而看起来是重复打印。如果要去掉重复可以这样做：
  - 在代码中添加一个判断语句，只在主进程中打印信息
  - 在Accelerate配置中设置更高的日志级别，这样可以减少非关键信息的重复打印
  - Accelerate提供了适合分布式环境的日志功能
- `在分布式训练中，虽然每个进程独立执行代码，但它们通过精心设计的机制紧密协作，形成一个协调一致的训练系统。Accelerate库和DeepSpeed通过抽象隐藏了这些复杂性，但底层仍然遵循这些原则实现高效协作训练。`这种设计允许模型大小超过单个GPU的内存限制，同时保持训练过程的一致性
   - 进程间通过专门的通信原语交换信息
   - **参数同步与梯度聚合**：每个进程计算模型的一部分梯度；通过all-reduce操作，每个进程的梯度被累加并同步回所有进程；所有进程使用相同的更新规则和累加梯度更新模型参数；
   -  数据并行与模型并行:`deepseep这几种的并行方式都有。`
      数据并行：每个进程拥有完整模型副本，处理不同数据批次
      ZeRO技术：模型参数、优化器状态和梯度在GPU间分片.`我用了zero3.`
      模型并行：模型层被分割到不同GPU（在Transformers中通常是注意力头分割）
      管道并行：模型在深度维度分割，不同层在不同GPU上





